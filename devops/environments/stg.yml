parameters:
  - name: artifactName
    type: string
    default: 'function-app'

  - name: packageFileName
    type: string
    default: 'function-app.zip'

  - name: runSmokeTest
    type: boolean
    default: false

  - name: smokeTestCommand
    type: string
    default: ''


stages:

  - stage: Deploy_STG
    displayName: Deploy STG

    dependsOn:
      - Deploy_UAT

    condition: succeeded()

    jobs:

      - template: ../templates/deploy/azure-function-deploy.yml

        parameters:
          environmentName: '<AZURE-DEVOPS-ENVIRONMENT-STG>'

          azureServiceConnection: '<AZURE-SERVICE-CONNECTION-STG>'

          functionAppName: '<FUNCTION-APP-NAME-STG>'

          artifactName: ${{ parameters.artifactName }}

          packageFileName: ${{ parameters.packageFileName }}

          appType: 'functionAppLinux'

          isFlexConsumption: false

          runSmokeTest: ${{ parameters.runSmokeTest }}

          smokeTestCommand: ${{ parameters.smokeTestCommand }}
