trigger: none

pr:
  branches:
    include:
      - main

name: ci-pr-$(Date:yyyyMMdd).$(Rev:r)


stages:
  - stage: Validate
    displayName: Validate Pull Request

    jobs:
      - template: ../templates/validate.yml
