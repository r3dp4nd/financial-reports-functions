name: CI - Pull Request

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

jobs:
  validate:
    uses: ./.github/workflows/reusable-validate.yml
    secrets: inherit
